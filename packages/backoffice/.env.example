# API base URL the backoffice talks to (shared with the player app's API).
VITE_API_URL=http://localhost:8100

# Where "open the player app" links go (the web app).
VITE_APP_URL=http://localhost:5173
